export const dayToCount = {
  xAccessor: d => d.day,
  yAccessor: d => d.count,
};

export const monthToCount = {
  xAccessor: d => d.month,
  yAccessor: d => d.count,
};

export const yearToCount = {
  xAccessor: d => d.year,
  yAccessor: d => d.count,
};

export const titleCount = {
  xAccessor: d => d.title,
  yAccessor: d => d.count,
};

export const dateCount = {
  xAccessor: d => d.date,
  yAccessor: d => d.count,
};
